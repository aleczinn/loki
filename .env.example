# Database
DATABASE_NAME="loki"
DATABASE_USER="NEED_TO_CHANGE"
DATABASE_PASSWORD="NEED_TO_CHANGE"

MEDIA_PATH=PATH_TO_MEDIA_DIR

# FFmpeg Hardware Acceleration
# auto  : Automatic (prefers GPU encoders if available)
# nvenc : Nvidia (requires NVIDIA GPU)
# qsv   : Intel Quick Sync (requires Intel iGPU or standalone GPU)
# vaapi : Video Acceleration API (Linux only, requires /dev/dri)
# amf   : AMD Advanced Media Framework (requires AMD GPU)
FFMPEG_HWACCEL=auto