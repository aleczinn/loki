# Database
DATABASE_NAME="loki"
DATABASE_USER="NEED_TO_CHANGE"
DATABASE_PASSWORD="NEED_TO_CHANGE"

MEDIA_PATH=PATH_TO_MEDIA_DIR

# FFmpeg Hardware Acceleration
# Options: auto (automatic detection), none, qsv (Intel), nvenc (NVIDIA), vaapi
FFMPEG_HWACCEL=auto

# NVIDIA GPU Configuration (optional)
# Wird automatisch alle GPUs erkennen
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=all